server {
    listen 80;
    server_name {{ item.domain_name }}.propentatech.com ;

    root /var/www/{{ item.domain_name }};
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }
}
